export interface UtopiaOptions {
  minWidth?: number
  maxWidth?: number
  minSize?: number
  maxSize?: number
  minTypeScale?: number
  maxTypeScale?: number
}

export type RequiredOptions = Required<UtopiaOptions>
